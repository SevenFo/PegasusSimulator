{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "setup_python_env",
            "type": "shell",
            "linux": {
                "command": "export CARB_APP_PATH=${workspaceFolder}/app/kit && export ISAAC_PATH=${workspaceFolder}/app && export EXP_PATH=${workspaceFolder}/app/apps && source ${workspaceFolder}/app/setup_python_env.sh && printenv >${workspaceFolder}/.vscode/.standalone_examples.env"
            }
        }
    ]
}
